# Copy to src/.streamlit/secrets.toml (when running from src/) or
# .streamlit/secrets.toml (when running from project root), then fill in your values.
# Do not commit the real secrets.toml file.

["DB_CREDENTIALS"]
DB_HOST = ""
DB_PORT = 3306
DB_USER = ""
DB_PASSWORD = ""
DB_NAME = ""
